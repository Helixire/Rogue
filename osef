void	(int **map, const int x, const int y, const int type)
{
	int	**tmp;
	int	cx;
	int	cy;

	init(type/dosize, &tmp, NULL);
	cy = - 1;
	while (++cy < 20)
	{
	cx = -1;
	while (++cx < 20)
	map[y * 20 + cy][x * 20 + cx] = tmp[cy][cx];
}
}